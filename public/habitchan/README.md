# Habit-Chan art drop 🧚‍♀️

Drop your generated expression PNGs **right here** (`app/public/habitchan/`). The app
serves this folder, so each file is reachable at `/habitchan/<name>.png`.

## Naming
Name each file by its **mood/pose** (lowercase, no spaces), e.g.:

- `happy.png`     — default / welcoming (waving hello)
- `excited.png`   — the fun beats (sparkly, big reaction — beads, bonus, wins)
- `proud.png`     — encouraging ("you did it!", "let's gooo")
- `talking.png`   — neutral explaining pose
- `thinking.png`  — curious / "hmm, let's set this up"
- `wink.png`      — playful / cheeky aside

You don't need all of these — send whatever you generated. Tell me the list of names
and I'll map them to the onboarding script (reusing the closest pose where one's missing).

Ideally **transparent background**, portrait-ish, similar size to each other. Any
resolution is fine. If they have backgrounds, say so and I'll handle it.
